#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_token() {
        // let token = Token::Number(42);
        // match token {
        //     Token::Number(n) => assert_eq!(n, 42),
        //     _ => panic!("Expected number token"),
        // }
    }
}